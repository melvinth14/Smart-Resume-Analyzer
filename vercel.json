{
  "version": 2,
  "buildCommand": "cd frontend && npm install && npm run build && cd ../backend && pip install -r requirements.txt && python -m spacy download en_core_web_sm || true",
  "builds": [
    {
      "src": "backend/main.py",
      "use": "@vercel/python"
    },
    {
      "src": "frontend",
      "use": "@vercel/static-build"
    }
  ],
  "routes": [
    {
      "src": "/api/(.*)",
      "dest": "backend/main.py"
    },
    {
      "src": "/(.*)",
      "dest": "frontend/index.html"
    }
  ]
}

